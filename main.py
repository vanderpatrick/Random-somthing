# Init user interface for the balance app
from datetime import time

import flet as ft


def main(page: ft.Page):
    t = ft.Text(color="green")
    page


if __name__ == "__main__":
    ft.app(target=main)
